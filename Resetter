-- Create a new ScreenGui instance
local newGui = Instance.new("ScreenGui")
newGui.Name = "NewGui"
newGui.ResetOnSpawn = false
newGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
newGui.DisplayOrder = 10

-- Helper function to add rounding
local function AddRounding(parent, radius)
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, radius)
    corner.Parent = parent
end

-- Create an ImageButton (Dark Gray & Rounded)
local newButton = Instance.new("ImageButton")
newButton.Name = "NewButton"
newButton.Size = UDim2.new(0, 40, 0, 40)
newButton.AnchorPoint = Vector2.new(0, 1)
newButton.Position = UDim2.new(0, 10, 1, -70)
newButton.Image = "rbxassetid://6031221087"
newButton.BackgroundColor3 = Color3.fromRGB(40, 40, 40) -- Dark Gray
newButton.Parent = newGui
AddRounding(newButton, 8) -- Adds rounded corners

-- Create a TextBox for inputting wait time (Dark Gray & Rounded)
local waitTimeInput = Instance.new("TextBox")
waitTimeInput.Name = "WaitTimeInput"
waitTimeInput.Size = UDim2.new(0, 100, 0, 30)
waitTimeInput.Position = UDim2.new(0, 10, 1, -120)
waitTimeInput.BackgroundColor3 = Color3.fromRGB(40, 40, 40) -- Dark Gray
waitTimeInput.TextColor3 = Color3.fromRGB(255, 255, 255) -- White text for contrast
waitTimeInput.PlaceholderText = "Wait time"
waitTimeInput.Text = "5.6734927"
waitTimeInput.Parent = newGui
AddRounding(waitTimeInput, 6) -- Adds rounded corners

-- Function to save the player's current position
local function SavePosition()
    local player = game.Players.LocalPlayer
    if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
        player:SetAttribute("SavedPosition", player.Character.HumanoidRootPart.Position)
    end
end

-- Function to teleport the player back
local function TeleportToSavedPosition()
    local player = game.Players.LocalPlayer
    local savedPosition = player:GetAttribute("SavedPosition")
    if savedPosition and player.Character then
        player.Character:MoveTo(savedPosition)
    end
end

-- Function to handle the button click
local function ButtonClicked()
    SavePosition()
    local player = game.Players.LocalPlayer
    if player.Character then
        player.Character:BreakJoints()
        local waitTime = tonumber(waitTimeInput.Text) or 5.6734927
        task.wait(waitTime)
        TeleportToSavedPosition()
    end
end

newButton.MouseButton1Click:Connect(ButtonClicked)
